Review the frontend code for Smartbox brand compliance.
Check:
1. All colors use CSS variables from main.css — no hardcoded hex values in HTML or JS
2. Font is Inter only — no other fonts used
3. All UI copy matches the brand voice rules in frontend/CLAUDE.md
4. CTA button, pills, cards, and progress bar follow component rules exactly
Report any violations with the file name and line number.
